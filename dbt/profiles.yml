randomuser_elt:
  target: main
  outputs:
    main:
      type: sqlite
      threads: 1
      database: 'database'
      schema: 'main'
      schemas_and_paths:
        main: 'dbt/data/randomuser.db'
      schema_directory: 'dbt/data'